import React from "react";
import { CheckCircle } from "lucide-react"; // ✅ Importing the check icon

const Check = () => {
    return <CheckCircle size={18} color="green" />;
};

export default Check; // ✅ Exporting the component properly
2 